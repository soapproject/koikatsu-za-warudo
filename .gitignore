# Reference repos (cloned for offline lookup, not part of this project)
references/

# Build output
bin/
obj/
*.user
*.suo
.vs/
runtime_inspect/

# Claude Code per-machine settings (may contain local paths and permissions).
# Project skills under .claude/skills/ ARE checked in (shared knowledge base).
.claude/*
!.claude/skills/
